_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_Store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea
.venv-scholar/
.scholar-browser-profile/
__pycache__/
*.pyc

# Local agent instructions. Never tracked — .claude/CLAUDE.md is the canonical
# copy of this content, and the two had already drifted (AGENTS.md pinned rbenv
# 3.3.7 against the repo's actual 3.3.11). Ignored so it stops surfacing in
# every status check; edit .claude/CLAUDE.md instead.
AGENTS.md